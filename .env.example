# Database Configuration
# Defaults to local SQLite file (file:local.db) if TURSO variables are omitted
TURSO_DATABASE_URL=
TURSO_AUTH_TOKEN=

# Default Local SQLite Database URL (used automatically in development if TURSO_DATABASE_URL is not set)
LOCAL_DATABASE_URL=file:local.db

# Environment
NODE_ENV=development
